# 2026-07-24 — Ayala (usagemonitoring-code-reviewer)

Reviewed `feature/mcp-server-packaging` diff vs `develop` (itermon v1.2.0 MCP packaging).
**Verdict: APPROVE** (rule-15 gate) — cleared to merge to `develop`.

## What I checked
- **Backward compatibility:** every change is additive — new `files` entry, new `bin`, new
  script, new keywords, description tweak, new README section. No existing tool behavior, CLI,
  or web-admin path changed. Safe for existing users. (This is a published package — BC is the bar.)
- **Version consistency:** `package.json` `version` and `iterm_mcp.py` `SERVER_INFO.version` both
  1.2.0; minor bump is correct semver for a new backward-compatible feature.
- **The one code change** (`sys.path.insert(realpath dir)` before `import iterm_ctl`) is necessary
  and correct: without it the symlinked global bin can't import its sibling module. It does not
  break the existing `.mcp.json` path invocation (same directory). Tester proved both paths.
- **Packaging hygiene:** `npm pack --dry-run` ships the new file and still excludes secrets/runtime
  artifacts. Bin file is executable.

## Non-blocking note
Unknown-tool `tools/call` → `-32603` rather than an `isError` result. Acceptable and pre-existing;
recommend the PM log a small future item to align with the softer MCP convention. Not a merge blocker.
