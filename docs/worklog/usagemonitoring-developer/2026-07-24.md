# 2026-07-24 — Gerrard (usagemonitoring-developer)

Implemented spec `docs/specs/mcp-server-packaging.md` on `feature/mcp-server-packaging`
(cut from `develop`), directed by Messi.

## Changes
- **package.json:** added `iterm_mcp.py` to the `files` allowlist; added `bin` entry
  `itermon-mcp` → `iterm_mcp.py`; added `npm run mcp` script; bumped `version` 1.1.4 → 1.2.0;
  added `mcp` + `model-context-protocol` keywords; mentioned MCP in `description`.
- **iterm_mcp.py:** synced `SERVER_INFO.version` to 1.2.0; inserted
  `os.path.dirname(os.path.realpath(__file__))` onto `sys.path` before `import iterm_ctl`
  so the global `itermon-mcp` bin (a symlink under `node_modules/.bin/`) resolves the module
  regardless of launch cwd. `chmod +x` the file for bin use.
- **README.md:** new "MCP server" section (tools table, `.mcp.json` setup for installed-bin and
  from-clone, `npm run mcp`, macOS permission + safety notes); Features bullet; Files-table row.

## Notes
- Kept it strictly additive — no change to any tool's behavior or the CLI/web admin. The
  `.mcp.json` path-based invocation (`python3 /abs/iterm_mcp.py`) still works unchanged since
  the realpath dir is the same directory.
- Handed to Dida for verification; passed review by Ayala; Messi merged to `develop`.
