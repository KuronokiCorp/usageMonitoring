# 2026-07-24 — Dida (usagemonitoring-tester)

Verified `feature/mcp-server-packaging` (itermon v1.2.0 MCP packaging). **Verdict: PASS.**
Covers BACKLOG #2 (verification pass over the MCP server).

## Safety first
This is a Linux CI host — **no iTerm2, no macOS, no `osascript`**, so no live user session can
possibly be touched by this run (the product's core safety rule is satisfied by construction).
`tools/call` therefore exercises dispatch and returns an `isError` result instead of acting on a
session — exactly the safe path I want to confirm.

## What I ran
1. **Packaging** — `npm pack --dry-run`: tarball now includes `iterm_mcp.py` (5.6 kB); the
   allowlist still excludes `.env`, `iterm_jobs.json`, `activity.log`. `package.json` is valid JSON;
   `version` 1.2.0; `bin.itermon-mcp` present.
2. **JSON-RPC handshake smoke test** — drove the server over stdin with
   `initialize` → `notifications/initialized` (notification) → `ping` → `tools/list` →
   `tools/call list_sessions` → unknown tool → unknown method. Results, ALL PASS:
   - notification gets no reply; exactly one reply per id.
   - `initialize` → `serverInfo {itermon, 1.2.0}` + tools capability.
   - `ping` → empty result.
   - `tools/list` → the three tools (`list_sessions`/`read_screen`/`send_command`), each with schema.
   - `tools/call list_sessions` → content array; engine failure surfaced as `isError:true`, no crash.
   - unknown tool → clean `-32603`; unknown method → `-32601`. Neither crashes the transport.
3. **Bin robustness** — symlinked `iterm_mcp.py` as `itermon-mcp` in an unrelated dir and ran it
   from `/`: `import iterm_ctl` still resolves and `tools/list` returns the tools. This proves the
   global-install bin actually works (without Gerrard's sys.path fix it would `ModuleNotFoundError`).

## Observation (non-blocking, for the PM's backlog)
Unknown-tool `tools/call` returns a JSON-RPC `-32603` error rather than an `isError` content
result (the softer MCP convention some clients prefer). Not a defect — pre-existing, out of scope.
