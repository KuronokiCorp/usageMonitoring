# 2026-07-24 — Lampard (usagemonitoring-release-manager)

**Process note:** this entry documents the release mechanics for itermon v1.2.0. The PM
(Messi) executed these steps directly this session, on a dispatch that arrived with the
release already CEO-approved and execution steps spelled out, rather than round-tripping
through a separate release-prep dispatch to me first. Recording the mechanics here so the
release-manager worklog stays the accurate place-of-record for release evidence, per rule 7.

## Release: itermon v1.2.0

- **Semver:** minor (1.1.4 → 1.2.0) — additive, backward-compatible (MCP server shipped
  alongside existing CLI/web-admin functionality, nothing removed or changed behaviorally).
- **Readiness inputs:** tester (Dida) PASS on `npm pack` contents + JSON-RPC smoke test;
  code-reviewer (Ayala) APPROVE — additive/backward-compatible, versions synced.
- **Merge:** `develop` → `main`, `--no-ff`, commit `74c2125`. Clean, no conflicts (verified
  merge-base first; `develop` never touched files also changed on `main`).
- **Dry-run (`npm publish --dry-run`):** 7 files packed — `LICENSE`, `README.md`,
  `iterm_ctl.py`, `iterm_mcp.py`, `iterm_web.py`, `package.json`, `start.sh`. Tarball
  `itermon-1.2.0.tgz`, 19.8kB packed / 62.5kB unpacked. `iterm_mcp.py` confirmed present in
  the `files` allowlist and in the packed tarball.
- **Auth:** local `npm whoami` → `shinan0528` (already logged in); `.env` also carries a
  non-empty `NPM_TOKEN` for the `publish:npm` script. No credential blocker.
- **Publish:** `npm publish` succeeded. Independently verified against the registry (not
  just CLI output): `npm view itermon version` → `1.2.0`; `npm view itermon dist-tags` →
  `{ latest: '1.2.0' }`; `npm view itermon versions` now includes `1.2.0`.
- **Rollback note:** no breaking change shipped, so rollback risk is low; if a defect
  surfaces, `npm deprecate itermon@1.2.0 "<reason>"` plus a patch release is the standard
  path — do not unpublish (npm policy discourages it and it can break installs pinned to
  the version).
- **Changelog:** no `CHANGELOG.md` exists in this repo. Release notes for this version live
  in `docs/briefs/2026-07-24-mcp-v1.2.0.md`, `docs/specs/mcp-server-packaging.md`, and this
  worklog. Not creating a new `CHANGELOG.md` unprompted — flagging to Messi as a candidate
  for a future backlog item / HQ proposal if the product wants one going forward.

## Unfinished / next
- If the product adopts a `CHANGELOG.md` convention, backfill v1.1.0–v1.2.0 from git log.
