# Release notes — itermon v1.4.0

**Status:** CEO-approval-conditional release, prepared by Lampard (usagemonitoring-release-manager).
Not merged to `main`, not tagged, not published. This document, the version bump, and the
finalized CHANGELOG are the prep artifact for the CEO's `develop`→`main` merge + `npm publish`
decision — see "CEO decision" at the end.
**Semver:** 1.3.0 → **1.4.0** (minor). See "Why minor, not major" below.

## Headline: closes a live remote-code-execution hole in the web admin (BACKLOG #12)

**itermon@1.3.0, currently live on npm, is vulnerable.** Before this fix, `iterm_web.py`
(`iterm-admin`, the local web admin) served `/api/send` and every other `/api/*` route with
**no `Origin` check, no `Host` check, and no authentication**. Any web page a user simply
*visited* — no interaction beyond loading the page — could POST a `text/plain`-labelled
request (a CORS "simple request": no preflight, no browser warning) and have arbitrary text
typed and Entered into **every one of the user's terminal sessions**, including any Claude
Code / AI-agent panes with live tool permissions. This is arbitrary command execution via a
drive-by webpage, not a theoretical weakness.

**If you run `itermon`'s web admin (`iterm-admin`), upgrade to 1.4.0 immediately.** If you only
use the CLI (`iterm-ctl`/`itermon`) or the MCP server, you were never exposed by this specific
hole — it is `iterm_web.py`-only.

### The fix: an `Origin` + `Host` allowlist, both fail-closed
- Checked on **every** request, on **every** HTTP verb, **before** the body is read and
  **before** any route dispatch — not something a future handler can accidentally bypass.
- `Host` header not on the allowlist → rejected (closes DNS rebinding too).
- `Origin` header present but not allowlisted, or `Origin: null` → rejected,
  `403 {"error":"forbidden"}`.
- Allowlist is always derived from the address the server is actually bound to — never a
  hard-coded port, so `--bind`/custom-port setups are covered automatically.
- **The one asymmetry the whole fix rests on, stated precisely:** a request with **no**
  `Origin` header at all is allowed. Browsers *always* attach `Origin` on a cross-origin
  request of this shape; `curl`/`wget`/`requests`/shell scripts never send one. This is why
  `curl -d '...' localhost:8765/api/send` keeps working unchanged and no token/auth scheme
  had to be introduced.
- New opt-in flag: `--allow-origin ORIGIN` (repeatable) for a LAN address browsed by
  hostname, or a future browser extension (`chrome-extension://<id>`) — validated at
  startup, refuses `*` and anything with a path component.
- Rejected-request logging is rate-limited (first rejection logged immediately, then at most
  once per 60s with a suppressed-count) so a drive-by attempt can't be turned into a disk-fill
  attack on the Activity log (BACKLOG #7/#11 lesson, applied here defensively).

### Honest limits — read this, don't just read the headline
- **This does not authenticate anything.** Any *other* local process on the same machine can
  still call the API with no `Origin` header and be obeyed, same as before. This closes the
  browser drive-by vector specifically, not "all unauthenticated local access."
- An XSS bug in the admin page itself would bypass this fix entirely (same-origin requests
  are, correctly, allowed).
- `Content-Type` is deliberately **not** validated — doing so would also break
  `curl -d '...'`, which sends no `Content-Type` browsers would recognize as "simple."

## Also in 1.4.0: the stable-job-targets / 0-match-as-failure line (BACKLOG #6)

Landed on `develop` the day before the RCE fix, riding the same unreleased line:

- **`index:` selector.** `iterm-ctl send index:2.1.1 "..."` used to silently match nothing
  (fell through every branch to the bare-substring fallback). It's now an explicit selector
  alongside `id:`/`tty:`/`name:`/bare-index — same exact-match semantics, fully additive.
- **A 0-match scheduled send is now a visible failure, not a silent success.** This was the
  root cause of a 20-day-silent broken job (BACKLOG #6): a rotted target used to log and
  report exactly like a legitimate empty send. It now logs `kind: "error"` and returns a
  distinct status (`"MATCHED 0 SESSIONS — not delivered"` from the scheduler; a new
  `"matched": 0` field from `/api/send`, with the existing `{"sent": N}` key and `200` status
  kept byte-identical for scripts that depend on them). A 1-or-more-match send is unchanged.
- **The admin UI stopped minting session-UUID (`id:`) targets for new scheduled jobs** — the
  actual root cause of the rot. New jobs now save `index:<n>` (survives session recreation in
  the same pane position). Manual-send stays on `id:` on purpose (no time window to rot in).
  Existing jobs already saved as `id:` are untouched and keep working.

## Why minor, not major (1.4.0, not 2.0.0)

- The security fix is a **new server-side restriction on who may call `/api/send`**, not a
  change to the request/response contract for a legitimate caller. Every caller that never
  sent a (bad) `Origin` header — every non-browser client that exists today — sees zero
  behavior change.
- `/api/send`'s existing `{"sent": N}` response key and `200` status are preserved exactly on
  both the happy path and the new 0-match path; the 0-match case only *adds* a `"matched"`
  field, it doesn't remove or repurpose anything.
- `resolve_targets`'s existing selector set (`id:`/`tty:`/`name:`/bare-index/`--all`) is
  unchanged; `index:` is additive.
- Nothing was removed. No existing function signature or CLI flag changed shape.
- This is a security **hardening**, and per this product's own semver discipline, breaking
  changes always land as major — this one doesn't break anything that was working correctly
  and unauthenticated-by-design use of the API was never a documented, supported contract.

## Verification performed for this release (in a `git worktree` off `develop`, not the live tree)

- `python3 tests/run_tests.py` → **92/93 passing.** The one failure is
  `test_ac14_packaging_unchanged_files_and_no_version_bump`, which asserts
  `package.json["version"] == "1.3.0"` — **an intentional AC written into the RCE feature spec
  (section 8) to prove the feature branch itself didn't sneak in a version bump.** Now that
  this release-prep step *is* the version bump, that specific assertion is expected to flip;
  every other assertion in that same test class (`G11OriginHostGuard` — the RCE guard's own
  AC-1 through AC-13, AC-D1/AC-D2) still passes. Not treating this as a red check — flagging
  it precisely so it reads as understood, not glossed over. Recommend Dida update this
  assertion (and `test_acd2_changelog_unreleased_has_security_section`, whose "next heading"
  lookup now spans the new `## [1.4.0]` section rather than isolating `## [Unreleased]`) on
  `develop` after this release ships, so they don't misfire against the next feature branch.
- `python3 tests/run_tests.py --twice` → two fresh-subprocess runs, **identical exit code (1)
  and identical summary both times** — deterministic, same single expected failure both runs.
- `python3 tests/counter_test.py` → baseline matches the run above (1 expected failure, same
  reason); **all 10 mutations caught** (M1–M8 carried over from 1.3.0's line, plus the two new
  ones for this release: **M9 neuters the Origin check, M10 neuters the Host check** — both
  caught, by the RCE guard's own AC tests directly, confirming the fix is real code the test
  suite actually exercises, not a check that happens to pass by construction). No surviving
  mutation. `git status --porcelain` identical before/after — no test droppings.
- `npm pack --dry-run` → **7 files** (`LICENSE`, `README.md`, `iterm_ctl.py`, `iterm_mcp.py`,
  `iterm_web.py`, `package.json`, `start.sh`) — same set as 1.2.0/1.3.0. **32.3 kB packed / 99.9
  kB unpacked** (up from 1.3.0's 27.4 kB — expected, `iterm_web.py` grew with the guard code).
  `CHANGELOG.md` correctly not in the tarball (unchanged PM ruling, still an open follow-up
  item, not this release).
- Registry before-state confirmed: `npm view itermon version` → `1.3.0`,
  `npm view itermon dist-tags` → `{ latest: '1.3.0' }`.
- RCE guard sanity-checked present in the packed source: `EXTRA_ALLOWED_ORIGINS`,
  `_forbidden()`, the Origin/Host guard block, all present in `iterm_web.py` on this tree.
- **`docs/specs/admin-api-origin-hardening.md` — present on this release-prep tree, recovered
  from `archive/pre-scrub-main` (see "Note on the spec file" below).**

## Note on the spec file — a discrepancy found and corrected, not silently assumed away

The release dispatch stated `main` "deliberately does not yet have the
`admin-api-origin-hardening.md` spec... merging develop→main will bring both the fix and the
spec." **I checked this independently rather than taking it on trust, and it does not hold in
the current git state:** `develop` never had this spec file in its own history — this repo's
convention (rule 12) is that record/spec docs are committed **directly to `main`** by the PM,
separately from the code that lands via `develop`/feature branches, not carried on `develop`
itself. The spec's original commit (`7b6bbc4`, authored by Messi 2026-08-15) only survives on
`archive/pre-scrub-main` — a backup of `main`'s pre-history-rewrite state, made before BACKLOG
#5's records scrub. The scrub appears to have dropped this spec from `main`'s current history
along with the worklogs/briefs it was targeting, and it was never re-added.

**Action taken:** recovered the file's content verbatim from `archive/pre-scrub-main`
(`git show archive/pre-scrub-main:docs/specs/admin-api-origin-hardening.md`) and staged it into
this release-prep branch. This is restoring an already-CEO's-chain-approved record, not
authoring new spec content — squarely release/records mechanics, not a call I made
unilaterally on substance. **Flagging to Messi explicitly:** please confirm this recovered copy
is correct and current before the merge — if the spec was meant to be revised (not just
restored) as part of this release, that's a PM/spec call, not mine.

## Rollback note

No breaking change ships in 1.4.0. If a defect surfaces after publish:
- **Do not `npm unpublish`** — company/npm policy; can break installs pinned to 1.4.0, and npm
  restricts it after 72 hours regardless.
- Standard path: `npm deprecate itermon@1.4.0 "<reason>"`, then ship a 1.4.1 patch.
- **If the Origin/Host guard itself is the defect** (e.g. a legitimate same-origin caller gets
  wrongly rejected): the immediate user-side workaround is `--allow-origin` naming the exact
  origin that needs to work, no code change required while a patch is prepared. Do **not**
  suggest disabling the guard entirely as a workaround — that reopens BACKLOG #12 and the
  vulnerability is what this release exists to close.
- If the stable-job-targets / 0-match change is the defect: it only changes logging/status
  strings and an additive JSON field; reverting the two commits (`3eaa404`, `4e9f988`) cleanly
  is low-risk if needed, independent of the security fix.

## Follow-up items opened by this release (not blocking)

- Ship `CHANGELOG.md` inside the npm tarball (add to `files`) — still deferred, unchanged from
  the 1.3.0 note.
- `test_ac14_packaging_unchanged_files_and_no_version_bump` and
  `test_acd2_changelog_unreleased_has_security_section` in `tests/run_tests.py` should be
  updated on `develop` post-release (see "Verification" above) so they don't misfire on the
  next feature branch.
- The `docs/specs/admin-api-origin-hardening.md` recovery above should be confirmed by Messi,
  and BACKLOG #5's scrub tooling should be checked for why it dropped a file outside its
  stated target set (worklogs/briefs/BACKLOG/agent defs) — a correctness gap in that tooling,
  not this release's problem to fix, but worth a pointer back to whoever owns it.

## CEO decision — the actual publish (rule 16, pick-and-submit)

Everything above is prepared on branch `release/1.4.0-prep` (worktree, off `develop`, **not**
merged, **not** pushed). The exact steps below are what the CEO is authorizing if he approves —
laid out so approval is a single informed yes, not a blank check.

**Recommended: Option 1 — merge, tag, publish, then restart the live server onto `main`.**

1. `git -C <main-worktree> merge --no-ff develop -m "Release itermon v1.4.0: promote develop to main"`
   — brings the RCE fix, the stable-job-targets work, this version bump, CHANGELOG, release
   notes, and the recovered spec onto `main` in one merge (verified clean merge-base, no
   conflicts expected — `main` and `develop` haven't diverged on any file this release touches
   except records, which follow rule 12's separate path).
2. Re-run all three verification checks (`run_tests.py`, `--twice`, `counter_test.py`,
   `npm pack --dry-run`) **on the merged `main` tree itself** — L5 retro lesson: a branch-time
   PASS is not trusted at merge time, full re-verification happens again, same as every prior
   release.
3. `git tag -a v1.4.0 -m "..." <merge-commit>` (local only, per standing practice — not pushed
   until the push-hold policy is separately resolved).
4. `npm publish` (via `npm run publish:npm`, the existing `.env`-sourced `NPM_TOKEN` script,
   unchanged) — **this is the one irreversible act, and it is the CEO's to authorize, not
   mine to take.**
5. Independently verify against the registry post-publish: `npm view itermon version` → should
   read `1.4.0`; `dist-tags` → `latest: '1.4.0'`.
6. **Restart the live server onto `main`.** The live launchd service already runs the fix
   (it's on `develop`, which already has the RCE guard) — this step is **branch hygiene, not a
   security fix reaching the CEO any faster**. Recommended anyway so the CEO's live
   configuration matches what real npm users are now running, rather than drifting further
   from it. `git worktree`, not `checkout`, for the swap — the working tree at
   `Products_usageMonitoring/` is launchd-managed live config; per BACKLOG's standing note,
   check it out onto `main` only if the CEO also wants the *checked-out branch* changed (a
   separate, lower-stakes call from the publish itself — can be deferred to a later session
   without any security cost).

**Alternative — Option 2: publish now, defer the live-server branch switch indefinitely.**
Same steps 1–5, skip step 6. Zero functional difference to security posture (the fix is
already live via `develop`); only difference is `main` and the running config stay on
different branches for longer, which is the exact drift BACKLOG has been flagging as a risk to
track, not urgent to close today.

**Alternative — Option 3: hold, do not publish this session.**
Not recommended — 1.3.0 on npm is a live, exploitable RCE for every current install; every
session this is held is a session real users stay exposed with no fix available to them via
`npm install`/`npm update`.

I do not execute any of steps 1, 3, 4, or 6 above without that explicit go — those are the
CEO-reserved outward/irreversible acts (merge to `main`, the publish itself, and — separately —
the push-hold question that already sits with Messi/CEO under BACKLOG #5). I can execute
steps 2 and 5 (re-verification, post-publish confirmation) as part of carrying out whichever
option is chosen.
