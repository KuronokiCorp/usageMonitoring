# Brief — itermon v1.2.0 MCP server packaging (2026-07-24)

**From:** Messi (usagemonitoring-product-manager) · **To:** chief-of-staff Zidane → CEO
**Session:** rule-13 standing-orders heartbeat.

## What moved
Shipped BACKLOG #1 (+ #2 folded in) through the full Git-Flow pipeline (rule 15):
`iterm_mcp.py` — the existing zero-dependency MCP stdio server (list_sessions / read_screen /
send_command) — is now packaged for npm users. Changes on `feature/mcp-server-packaging`,
merged to `develop`:
- `package.json`: `iterm_mcp.py` added to `files`; new `itermon-mcp` bin; `npm run mcp`;
  version **1.1.4 → 1.2.0**; mcp keywords + description.
- `iterm_mcp.py`: version synced to 1.2.0; symlinked-bin import fix.
- `README.md`: MCP server section (tools, `.mcp.json` setup, safety).

Also stood up the rule-7 records infrastructure the repo never had: `docs/worklog/<agent>/`
for all roles + `docs/briefs/`, `docs/specs/`.

## Gate status
- Tester (Dida): **PASS** — npm pack ships the file, secrets excluded; JSON-RPC handshake
  smoke test all-pass; symlinked-bin import proven. Safe: no iTerm2 on the host, no live
  session touched.
- Code review (Ayala): **APPROVE** — additive, backward-compatible, versions synced.
- Merged to `develop`. **Not** merged to `main`; **not** published — both CEO-gated.

## Pushed to
`develop` + `feature/mcp-server-packaging` (code); `main` (records: spec, worklogs, brief,
BACKLOG). All to `origin`.

## Needs a CEO decision (rule 16)
**Release itermon v1.2.0 to npm?** — see the pick-and-submit question in BACKLOG.md and below.
Recommended: **Approve release** (promote `develop`→`main`, Lampard preps changelog/tag,
publish). itermon is stable, the change is additive and green at both gates.
